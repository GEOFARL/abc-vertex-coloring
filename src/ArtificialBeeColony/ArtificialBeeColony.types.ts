export interface ArtificialBeeColonyConfig {
  employedBeesCount: number;
  onlookerBeesCount: number;
  verbose?: boolean;
}

export interface ArtificialBeeColonyLoggerOptions {
  array?: number[];
}
